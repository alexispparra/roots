import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function PrioritizePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="font-headline">AI Priority</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This section is under construction.</p>
      </CardContent>
    </Card>
  );
}
